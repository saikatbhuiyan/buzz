# fieldbuzz

### Project setup

```
mkdir fieldbuzz
cd fieldbuzz
python -m venv venv
source venv/bin/activate
.\venv\Scripts\activate.bat (Windows)
git clone git_url
cd buzz
create a postgres db with name `recruitment`
run python manage.py makemigrations
run python manage.py migrate
run python manage.py migrate
```